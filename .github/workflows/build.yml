name: Build 5Mind Desktop

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build for ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: windows-latest
            platform: win
          - os: macos-latest
            platform: mac

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Only needed for Linux pacman target
      - name: Install bsdtar (libarchive-tools) for Pacman
        if: matrix.platform == 'linux'
        run: sudo apt-get update && sudo apt-get install -y libarchive-tools

      - name: Build application
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ matrix.platform }}" = "linux" ]; then
            npx electron-builder --linux appimage deb pacman rpm --x64
          elif [ "${{ matrix.platform }}" = "win" ]; then
            npx electron-builder --win --x64
          elif [ "${{ matrix.platform }}" = "mac" ]; then
            npx electron-builder --mac --x64 --arm64
          fi

          echo "=== Built files in dist/ ==="
          ls -la dist/

      - name: List built artifacts
        shell: bash
        run: |
          echo "Listing artifacts for ${{ matrix.platform }}:"
          ls -la dist/* || true

      - name: Upload artifacts (for debugging)
        uses: actions/upload-artifact@v4
        with:
          name: 5mind-desktop-${{ matrix.platform }}
          path: dist/*
          retention-days: 7

      - name: Create Release & Upload Assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: 5Mind Desktop ${{ github.ref_name }}
          body: |
            ### 5Mind Desktop ${{ github.ref_name }}

            Cross-platform release:
            - **Windows** — Installer + Portable
            - **macOS** — DMG + Universal ZIP (x64 & arm64)
            - **Linux** — AppImage, DEB, Pacman, RPM

            Auto-generated changelog below.
          generate_release_notes: true
          draft: false
          prerelease: false
          fail_on_unmatched_files: false   
          files: |
            dist/*.exe
            dist/*.portable.exe
            dist/*.zip
            dist/*.dmg
            dist/*.AppImage
            dist/*.deb
            dist/*.pacman
            dist/*.rpm
