name: Build 5Mind Desktop

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write  # Needed to create releases and upload assets

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Helpful for generate_release_notes

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'  # Current LTS as of Dec 2025 (Node 18 is EOL)
          cache: 'npm'

      - name: Install dependencies
        run: npm ci  # Cleaner and faster than npm install in CI

      - name: Build AppImage (electron-builder)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Usually not needed, but kept if you use it elsewhere
        run: |
          npx electron-builder --linux appimage --x64
          echo "AppImage files:"
          ls -la dist/*.AppImage

      - name: Build DEB (Electron Forge)
        run: |
          npx electron-forge make --platform linux --arch x64 --targets @electron-forge/maker-deb
          echo "DEB files:"
          ls -la out/make/*.deb

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: 5Mind Desktop ${{ github.ref_name }}
          generate_release_notes: true
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            dist/*.AppImage
            out/make/*.deb
